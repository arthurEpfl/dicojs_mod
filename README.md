# Tailored Disco.js lib for Antibiogo

## Install and build

Type the following commands to install the Node.js modules and build the project. Built files are located in the `dist/` dir.

```
npm ci
npm run build
```
